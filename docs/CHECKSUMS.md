# Checksums

`checksums.sha256` records source-file SHA-256 hashes.

Release ZIP checksums should be published next to the ZIP as:

```text
opencode-rtk-skill-v4.0.0.zip.sha256
```

Do not hard-code the final ZIP hash inside the ZIP. Changing a file inside the ZIP changes the ZIP hash.
